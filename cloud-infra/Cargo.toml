[package]
name = "cloud-infra"
version = "0.1.0"
edition = "2024"

[dependencies]
cloud-infra-core = { path = "../cloud-infra-core" }
serde = { version = "1.0.219", features = ["serde_derive", "derive"] }
serde_json = "1.0.142"
